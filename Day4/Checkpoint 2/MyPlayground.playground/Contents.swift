import Cocoa

/* Checkpoint 2 */

let names = ["Nicholas", "James", "Caidyn", "Brandon", "Brandon", "Brandon"]

print("Number of items in names array: \(names.count).")

let uniqueNames = Set(names)

print("Number of unique items in names array: \(uniqueNames.count).")
